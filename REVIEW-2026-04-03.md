# Lutris.ai Code Review — 2026-04-03

## Sync Status

Local (`C:\Users\admin\.openclaw\workspace\projects\lutrisai\lutrisai`) and GitHub (`chancelu/lutris.ai`) are **identical** at commit `ffdfb61` (fix: persist AI chat messages on completion and before page unload). No uncommitted local changes.

## Test Results

| Type | Result |
|------|--------|
| Unit (Bun, 55 files) | **1212 pass**, 0 fail, 40 skip |
| E2E (Playwright, 28 specs) | 27 fail — dev server not running (`localhost:1420` required) |
| Lint (oxlint, 227 files) | 6 errors, 9 warnings |

## Findings

### CRITICAL

| ID | Issue | File | Line |
|----|-------|------|------|
| C1 | `VITE_` prefixed API keys bundled into client JS | `.env.local` | 2, 6 |
| C2 | API keys sent directly from browser to third-party APIs | `use-image-gen.ts`, `use-chat.ts` | 6-9, 269 |
| C3 | Client can override system prompt via `body.system` | `api/chat.js` | 55 |
| C4 | Prompt injection backdoor ("CRITICAL INSTRUCTION" magic string) | `use-chat.ts` | 58 |

### HIGH

| ID | Issue | File | Line |
|----|-------|------|------|
| H1 | Wildcard CORS `Access-Control-Allow-Origin: *` | `api/chat.js` | 16 |
| H2 | No rate limiting on AI proxy | `api/chat.js` | — |
| H3 | XSS via `v-html` with unsanitized markdown | `ProductDocPanel.vue` | 113 |
| H4 | Blob URL memory leak (never revoked) | `src/ai/tools.ts`, `use-image-gen.ts` | 118, 86 |
| H5 | No timeout on AI fetch | `api/chat.js` | 75 |
| H6 | Stream error: no check on `aiRes.ok` before `.getReader()` | `api/chat.js` | 85 |
| H7 | Event listener leak on graph replacement | `stores/editor.ts` | 831 |
| H8 | No request body validation on API proxy | `api/chat.js` | 35 |

### MEDIUM

| ID | Issue | File | Line |
|----|-------|------|------|
| M1 | `editor.ts` 1955 lines — god object | `stores/editor.ts` | — |
| M2 | `use-canvas-input.ts` 1417 lines | `composables/use-canvas-input.ts` | — |
| M3 | `onAuthStateChange` never unsubscribed | `use-auth.ts` | 25 |
| M4 | Direct mutation of `parent.childIds` bypasses event system | `editor.ts` | 1075+ |
| M5 | Autosave silently swallows all errors | `editor.ts` | 657 |
| M6 | `enableEval` defaults to `true` in MCP server | `mcp/src/server.ts` | 70 |
| M7 | Hardcoded viewport 800x600 in `goToMainComponent` | `editor.ts` | 1338 |

### LOW

| ID | Issue | File | Line |
|----|-------|------|------|
| L1 | Gemini key uses legacy `designflow-gemini-key` localStorage key | `use-image-gen.ts` | 7 |
| L2 | `wrapInAutoLayout` undo/redo creates new node ID on redo | `editor.ts` | 1013 |
| L3 | Missing CSP, HSTS, Permissions-Policy headers | `vercel.json` | — |
| L4 | XSS via `v-html` with Prism output (lower risk) | `CodePanel.vue` | 263 |

### Lint Errors

| File | Issue |
|------|-------|
| `packages/core/src/tools/describe.ts:135` | `detectStructuralIssues` complexity 22 > 20 |
| `src/composables/use-chat.ts:482` | TS2322 type mismatch on `tools` |
| `src/composables/use-keyboard.ts:17` | Should use optional chain |
| `packages/core/src/tools/icons.ts:176` | Unnecessary condition (always falsy) |
| `packages/core/src/tools/ai-adapter.ts:110` | Unnecessary condition (always truthy) |
| `packages/core/src/tools/patterns.ts:186` | Unnecessary optional chain |

## Fix Plan

### Phase 1 — CRITICAL (immediate)
1. Remove prompt injection backdoor from system prompt
2. Restrict CORS to production domain
3. Stop leaking error details to client
4. Ignore client-supplied system prompt (use server-side constant or prepend fixed prefix)
5. Add request body validation
6. Add fetch timeout and stream error handling

### Phase 2 — HIGH (this session)
7. Sanitize markdown output with DOMPurify before `v-html`
8. Revoke blob URLs when no longer needed
9. Unsubscribe event listeners on graph replacement
10. Add `AbortSignal.timeout()` to AI fetch

### Phase 3 — MEDIUM (this session)
11. Change `enableEval` default to `false`
12. Fix auth subscription leak
13. Add autosave error feedback
14. Fix lint errors

### Phase 4 — Deferred
15. Split `editor.ts` and `use-canvas-input.ts` (large refactor)
16. Add rate limiting (requires Upstash/KV setup)
17. Add CSP headers
18. Migrate legacy localStorage keys

---

## Fix Results (2026-04-03)

### Applied Fixes

| ID | Fix | File |
|----|-----|------|
| C3 | Removed client system prompt passthrough | `api/chat.js` |
| C4 | Removed "CRITICAL INSTRUCTION" prompt injection backdoor | `use-chat.ts` |
| H1 | Restricted CORS to known origins | `api/chat.js` |
| H3 | Added HTML entity escaping before markdown rendering | `ProductDocPanel.vue` |
| H4 | Added blob URL tracking and `revokeAll()` cleanup | `use-image-gen.ts` |
| H5 | Added `AbortSignal.timeout(55000)` to AI fetch | `api/chat.js` |
| H6 | Added `aiRes.ok` check before streaming | `api/chat.js` |
| H7 | Fixed event listener leak using nanoevents unbind pattern | `stores/editor.ts` |
| H8 | Added request body validation (messages array, max_tokens) | `api/chat.js` |
| M3 | Fixed auth subscription leak with unsubscribe tracking | `use-auth.ts` |
| M5 | Added toast notification on autosave failure | `stores/editor.ts` |
| M6 | Changed `enableEval` default to `false` | `mcp/src/server.ts` |
| Lint | Fixed complexity in `detectStructuralIssues` (extracted helper) | `tools/describe.ts` |
| Lint | Fixed TS2322: separated `commitAIBatch` from tools object | `ai/tools.ts`, `use-chat.ts` |
| Lint | Fixed optional chain lint warning | `use-keyboard.ts` |
| Lint | Fixed unnecessary condition in `patchEnumType` | `tools/ai-adapter.ts` |
| Error | Removed error detail leakage to client | `api/chat.js` |

### Remaining (2 lint errors — pre-existing, not fixable without `noUncheckedIndexedAccess`)
- `icons.ts:177` — `no-unnecessary-condition` on Record index (defensive check)
- `patterns.ts:187` — `no-unnecessary-condition` on Record index (defensive check)

### Verification
- Unit tests: **1212 pass, 0 fail** (unchanged)
- Lint errors: **6 → 2** (4 fixed, 2 are false positives on defensive Record checks)
- Lint warnings: **9** (unchanged, all pre-existing)
