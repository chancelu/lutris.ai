你是 Lutris.ai 项目的前端工程师。请阅读 docs/PRD-R9-UI-LIGHTEN.md，这是一份 UI 减重 PRD。

按照 PRD 的优先级顺序逐项执行：
1. 右侧面板重构（PropertiesPanel.vue / ChatPanel.vue）
2. 左侧面板减重（LayersPanel.vue / EditorView.vue）
3. Toolbar 极简化（Toolbar.vue）
4. WelcomeOverlay 精简（WelcomeOverlay.vue）
5. 全局视觉减重（各组件 border/圆角/背景统一）

执行规则：
- 每完成一个大项，确认 build 没 break
- 不要动 EditorCanvas、AI Chat 逻辑、Collab、路由
- 保持所有功能可用，只是视觉和布局的精简
- 用 Tailwind CSS 类操作，不要引入新的 CSS 文件

完成后运行 npx vite build 确认构建通过。
