You are working on Lutris.ai, a Vue 3 + Tailwind CSS 4 + Skia WASM AI design tool based on OpenPencil.

TASK: Fix all known issues in this project. Do them ALL in sequence:

1. Fix bun run build exit 1
- npx vite build works fine (exit 0) but bun run build exits 1
- Investigate package.json scripts and fix the issue

2. Fix 4 failing tests
- 3 ACP agent tests + 1 CJK font fallback test
- Run: bun test ./tests/engine
- Find and fix the root causes

3. Fix 28 Playwright e2e tests being loaded by bun test
- These should only run via playwright test, not bun test
- Fix the test config so bun test excludes playwright e2e files

4. Fix 41 lint warnings
- Run: npx oxlint -c oxlint.json --type-aware --type-check src/ packages/core/src/ packages/cli/src/ packages/mcp/src/ packages/acp/src/
- Fix all warnings to get to 0 warnings

5. Fix type errors
- Run: npx tsgo --noEmit
- Fix ALL type errors
- Known issue: packages/core/src/index.ts may be missing export for buildTailwindConfig

6. Landing Page animation enhancement
- Add scroll parallax effects to the landing page
- Add feature card entrance animations (fade-in, slide-up on scroll)
- Use motion-v (already in dependencies) for animations
- The landing page component is in src/views/ directory

7. Bundle size optimization
- Main bundle index-B-DH-lJA.js is 2427KB
- Look for opportunities to lazy-load heavy dependencies
- Consider dynamic imports for less-used features like mammoth, fontoxpath, html2canvas
- Target: reduce main bundle by at least 20%

After each fix, verify it works by running the relevant command.
Commit each fix separately with descriptive commit messages.

When completely finished, run this command:
openclaw system event --text "Done: Fixed all Lutris.ai issues" --mode now
