You are working on Lutris.ai, a Vue 3 + Tailwind CSS 4 + Skia WASM AI design tool.

All bugs are already fixed. Now do these two enhancement tasks:

## 1. Landing Page Animation Enhancement
- File is likely at src/views/LandingPage.vue
- Add scroll parallax effects to hero section
- Add feature card entrance animations (fade-in + slide-up on scroll)
- Use motion-v (already in package.json dependencies) for animations
- Keep it tasteful and performant, not over-the-top

## 2. Bundle Size Optimization
- Current main bundle index.js is 2428KB
- Already split: vendor-ai, vendor-mammoth, vendor-markdown, vendor-pdf, vendor-reka, fontoxpath, html2canvas, vendor-vueuse
- Look for more opportunities to lazy-load or dynamic import heavy deps
- Check if any large deps can be moved to async chunks
- Target: reduce main bundle by at least 15-20%
- Verify with: npx vite build

After each task, verify it works. Commit each separately.

When completely finished, run:
openclaw system event --text "Done: Landing page animations + bundle optimization" --mode now
